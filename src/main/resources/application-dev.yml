spring:
  thymeleaf:
    cache: false

# Compte ADMIN créé automatiquement au démarrage en dev par DevUtilisateurSeeder :
# admin@lacassinauteur.local / admin123 (jamais utilisé en prod, cf. ce seeder).

logging:
  level:
    fr.lacassinauteur.site: DEBUG
