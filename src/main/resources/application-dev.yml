spring:
  thymeleaf:
    cache: false
  # Utilisateur en mémoire temporaire pour le développement local, le temps que le
  # domaine `identity` (Phase 1 de la roadmap) fournisse une authentification réelle.
  # A supprimer dès que ce domaine est implémenté.
  security:
    user:
      name: admin
      password: admin
      roles: ADMIN

logging:
  level:
    fr.lacassinauteur.site: DEBUG
